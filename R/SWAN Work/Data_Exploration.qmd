---
title: "Data_Exploration"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

```{r}
data = da28762.0001
head(data)

library(tidyverse)
library(dplyr)
library(stats)
library(car)
library(caret)

View(data)
```

```{r}
data %>%
  select()
```

```{r}
# glimpse(data)

ID <- na.omit(data$SWANID)
status0 <- na.omit(data$STATUS0)
spbmd <- na.omit(data$SPBMDDT0)
hpbmd <- na.omit(data$HPBMDDT0)
ojcalc <- na.omit(data$OJCALC0)
yrsca <- na.omit(data$YRSCA0)
supca <- na.omit(data$SUPCA0)
dttcalc <- na.omit(data$DTTCALC0)
allcalc <- na.omit(data$ALLCALC0)


data <- data.frame( ,)
```

```{r}
linear.model <- lm()
```
